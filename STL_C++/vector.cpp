#include <bits/stdc++.h>
using namespace std;

/*
int main() {
    vector<int> v;
    v.push_back(10);
    v.push_back(20);
    v.push_back(40);
    cout<<"size of vector : "<<v.size()<<endl;
    cout<<"The elemetns of vector are : "<<endl;
    for(int x: v){
        cout<<x<<endl;
    }
    return 0;
}   
*/
/*
int main() {
    vector<int> v = {40, 10, 70, 20, 50};
    int mx = v[0];
    int mn = v[0];
    for(int i = 1; i < v.size(); i++) {
        mx = max(mx, v[i]);
        mn = min(mn, v[i]);
    }
    cout << "Maximum = " << mx << endl;
    cout << "Minimum = " << mn << endl;
    return 0;
}
    */
